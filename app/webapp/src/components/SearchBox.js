import React from 'react';

export default () => (
    <input
        type="search"
        id="site-search"
        name="q"
        placeholder="Search restrooms..."
        aria-label="Search through site content"
    />
);
